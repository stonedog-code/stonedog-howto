node_modules/
.next/
coverage/
dist/
out/
*.log
.DS_Store
.env
.env.local
content-cache/

# Generated by Next.js on every build.
.next/
next-env.d.ts

# A tsc incremental cache. It was tracked, so it churned on every build and
# every diff carried a binary-ish blob nobody reads.
tsconfig.tsbuildinfo

# Playwright
test-results/
playwright-report/

# PRDs live in the INTERNAL stonedog-prd repo, never here. `docs/prd` is a
# symlink into it, so the documents are where a person expects while this
# repository commits none of them. See the StoneDogCode CLAUDE.md.
docs/prd

# Your own article-tree configuration. Copy articles.example.json.
articles.json
