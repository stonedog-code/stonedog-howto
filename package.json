{
  "name": "stonedog-howto",
  "version": "0.1.0",
  "description": "A how-to documentation surface for applications: markdown articles that describe themselves in frontmatter, section manifests, per-article tables of contents, search, and access control supplied by the host.",
  "license": "Apache-2.0",
  "author": "StoneDogCode L.L.C.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stonedog-code/stonedog-howto.git"
  },
  "type": "module",
  "sideEffects": false,
  "//exports": "This package ships TypeScript SOURCE, not a bundled dist/, for the same reason stonedog-style does: the React layer is styled with Panda CSS, which extracts styles by statically parsing source at the CONSUMER's build. A pre-bundled dist would emit class names the consumer's Panda never generated a stylesheet for. Consumers transpile us (Next.js: `transpilePackages: ['stonedog-howto']`) and add our src to their panda `include` globs.",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "files": [
    "src",
    "LICENSE",
    "NOTICE",
    "README.md"
  ],
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint .",
    "//test": "Jest runs as ESM (see jest.config.cjs), which needs the VM modules flag.",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "gate": "npm run type-check && npm run lint && npm run test",
    "version:bump:patch": "npm version patch --no-git-tag-version",
    "version:bump:minor": "npm version minor --no-git-tag-version",
    "audit": "npm audit --audit-level=moderate"
  },
  "dependencies": {
    "github-slugger": "^2.0.0",
    "mdast-util-to-string": "^4.0.0",
    "remark-gfm": "^4.0.0",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.0",
    "unist-util-visit": "^5.0.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/jest": "^29.5.14",
    "@types/mdast": "^4.0.4",
    "@types/node": "^22.10.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.17.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.11",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "documentation",
    "markdown",
    "how-to",
    "table-of-contents",
    "rbac"
  ]
}
